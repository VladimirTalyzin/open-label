[Unit]
Description=OpenLabel Service
After=network.target

[Service]
User=morfo
Group=morfo
WorkingDirectory=/open-label
ExecStart=uvicorn main:app --host 0.0.0.0 --port 8001
Restart=always

[Install]
WantedBy=multi-user.target
